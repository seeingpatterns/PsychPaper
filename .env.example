# Server (로컬 개발 시 server 폴더에서 npm run dev 할 때)
PORT=3000
NODE_ENV=development

# Database — app role from db-schema/11_db_roles.sql (not bootstrap POSTGRES_USER)
DATABASE_URL=postgresql://psychpaper_app:<app-password>@localhost:5432/psychpaper

# Admin JWT (나중에 추가)
# JWT_SECRET=your-secret-key
