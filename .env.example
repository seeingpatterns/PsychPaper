# Server (로컬 개발 시 server 폴더에서 npm run dev 할 때)
PORT=3000
NODE_ENV=development

# Database (서비스 계정 사용; 실제 비밀번호로 교체)
DATABASE_URL=postgresql://psychpaper_app:your_app_password@localhost:5432/psychpaper

# Admin JWT (나중에 추가)
# JWT_SECRET=your-secret-key
